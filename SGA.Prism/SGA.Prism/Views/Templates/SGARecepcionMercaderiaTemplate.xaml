<?xml version="1.0" encoding="utf-8" ?>
<ContentView 
            xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SGA.Prism.Views.Templates.SGARecepcionMercaderiaTemplate">

    <ContentView.Content>
        <StackLayout>
            <StackLayout Orientation="Vertical" Margin="10,10,10,10">
                <Label VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" Text="{Binding NombreConductor}" TextColor="Black"/>
                <StackLayout Orientation="Horizontal">
                    <Label VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" Text="{Binding Almacen}" />
                    <Label VerticalOptions="CenterAndExpand" HorizontalOptions="EndAndExpand" Text="{Binding OrdenInterna}" TextColor="Red"/>
                </StackLayout>

            </StackLayout>
            <StackLayout
                    Margin="0,0,10,0"
                    HorizontalOptions="FillAndExpand"
                    Orientation="Horizontal"
                    VerticalOptions="Center">
                <BoxView
                        HeightRequest="0.3"
                        HorizontalOptions="FillAndExpand"
                        Color="LightGray" />
            </StackLayout>
        </StackLayout>
    </ContentView.Content>
</ContentView>